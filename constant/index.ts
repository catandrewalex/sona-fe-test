// fee that must be pay when late per day
export const PaymentLateBaseFeeMultiplierPerDay = 10000;

// payment must be done before day 10 of each month before it is marked as late
export const PaymentLastDatePerMonth = 10;
